def GetCDpay(UEFC, opt_vars, AR, S):
    AR = opt_vars[3]
    S = opt_vars[4]

    # Return the drag contribution from the payload in coefficient form
    #
    # CDpay = Dpay / (q S)  where S = wing planform
    CDpay = 0.0
    
    return CDpay

